# $Id$
# CPSCore TODO 

- Packaging
   o move all skins into CPSDefault/skins
   o remove CPSDummyDocument
   o move CPSFolder.py into CPSDefault/Folder.py
   o remove zexp workflow
   o update INSTALL.txt
   o make _ut work on CPSCore
   o test screnarii use ZTC ? 

# still up to date ???
- Elements
   o CachableElement
   o patches for ZPT and Python Scripts
- Events
  o use the new API (see notes.txt)
